# 2026-06-28 易道智鉴开发日志

## 修复项（共12项）

### 关键Bug修复
1. **八字排盘报错** — `STEMS`/`BRANCHES`/`ELE`/`ZHI_ELE` 未在全局作用域定义，computeBazi() 引用时抛 ReferenceError。在 line 118-121 添加全局 const 定义。
2. **农历↔公历转换错误** — `solarToLunar` 用 `solarYear-1900` 偏移量查表，但 LUNAR_NEW_YEAR 等表用完整年份做key。`lunarToSolar` 春节日期编码也有bug（值>20为1月，≤20为2月）。重写两个函数。
3. **renderLiuYueModule jiEle 未定义** — 调用时未传 jiEle 参数。补全。
4. **renderNewBaziModules hour 未定义** — 解构时漏了 hour。补全。
5. **8个排盘按钮函数不存在** — `runQimen`/`runZiwei`/`runMeihua`/`runLiuren` 及4个Engine版本函数在HTML onclick中被调用但从未定义。添加8个wrapper函数指向 `computeQimen`/`computeZiWei`/`computeMeiHua`/`computeLiuRen`。

### 功能增强
6. **6种罗盘专业解读** — 奇门(九宫/九星/八门/八神/三奇六仪)、六壬(四课/三传/天将/参断)、玄空(九星/方位/化解)、八宅(四吉四凶/宅命配/五行化煞)、河图洛书(五行局/数理应用)、六爻(位次/六亲/六神/断卦要诀)
7. **阳历输入提醒** — 日期框旁新增提示文字
8. **会员等级** — 圆满→通达（3处替换，0残留）

### 此前已修复（本次验证通过）
9. 主平台9张工具卡片导航
10. 出生地/现居地输入框 + 城市经度自动推算
11. 起心动念时辰输入框
12. 精准择日7层推演引擎

## 备份文件
- `app/js/divination-core.js.bak.20260628-compass`
- `app/js/divination-core.js.bak.20260628-liuren-upgrade`

## 全量测试结果
- 历法转换: 6/6 通过
- 八字排盘(8日期跨1950-2050): 8/8 通过
- 关键函数: 31/31 定义
- 罗盘函数: 9/9 定义
- 罗盘内容: 6/6 增强完成
- HTML元素: 32/32 存在
- JS语法: 12/12 文件通过
- 页面HTTP: 3/3 返回200

## 第三轮修复 (16:00-16:23)

### onclick未定义函数修复 (13个)
1. `exchangeVip()` (wechat-hub) → `showExchangeModal()`
2. `runLiuyaoEngine()` → `yjStart('person')`
3. `runFengshuiEngine()` → `computeFengshui()`
4. `runXingmingEngine()` → `analyzeName()`
5. `runZeriEngine()` → `runPrecisionZeRi()`
6-13. 之前修复的8个排盘wrapper函数

### 黄历每日建议修复
- `buildDailyComprehensiveAdvice` 输出被 `faithEl.innerHTML=faithHtml` 覆盖 → 改为 `+=`
- `divination-almanac.html` 硬编码宜忌建议 → 替换为7维度动态生成(黄历宜忌+干支五行+节气+建除+冲煞+值神+个人八字)
- `divination-almanac.html` `Math.random()>0.3` 硬代码 → 改为基于黄历宜项匹配

### 后台管理修复
- 引擎测试台 `formatPaipanResult` bug: `html+'...'` → `html+='...'` (JSON部分不显示)
- 排盘结果格式适配实际API返回字段(pillars/day_master/wuxing_score/shensha/dayun)
- 排盘服务(8911端口)启动
- 知识库计数逻辑修正: 只统计≥50字符的字符串为知识条目(之前单字"甲"也算卡片)

### 强制刷新
- 移除自动reload循环脚本(导致页面不停刷新)
- 所有JS时间戳统一更新

### 知识库补充 (进行中)
- 945条知识条目需扩展到1000+字符
- 3个子Agent并行处理16个领域
- 第1组: bazi/xingming/ziwei/qimen/wuxing
- 第2组: liuyao/meihua/liuren/liushisigua/bagua
- 第3组: fengshui/tizhi/shishen/nayin/shensha/hechong
