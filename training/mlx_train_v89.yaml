# v8.9 SFT 训练配置 — 修真 v8.8 稀释问题
# v8.8 失败：自由问答补足 400 → BaziQA 推理链占比 64%→39% → 41.2%（-10.6pt）
# v8.9 修真：推理链 390 全量 + 自由问答 100（20%）+ R733 19 = 507 条
#
# 训练链路：Qwen2.5-3B base → v8.7 fused → v8.9 LoRA

model: "/Users/tom/.openclaw-autoclaw/workspace/projects/mingli-baojian/training/mlx-models/mingli-sft-v8.7-7b"
train: true
test: false

train_file: "train.v89.jsonl"
valid_file: "valid.v89.jsonl"
data: "/Users/tom/.openclaw-autoclaw/workspace/projects/mingli-baojian/training/mlx-r105-data-v89"

fine_tune_type: "lora"
num_layers: 20
lora_parameters:
  rank: 4
  dropout: 0.05
  scale: 4.0

iters: 200
batch_size: 1
grad_accumulation_steps: 4
learning_rate: 5e-6
optimizer: "adamw"
optimizer_config:
  adamw:
    betas: [0.9, 0.999]
    eps: 1e-8
    weight_decay: 0.01

max_seq_length: 1024
mask_prompt: true
grad_checkpoint: true

save_every: 50
steps_per_eval: 50
steps_per_report: 10
val_batches: 20
seed: 42

adapter_path: "/Users/tom/.openclaw-autoclaw/workspace/projects/mingli-baojian/training/mlx-checkpoints/mingli-sft-v89-7b"
