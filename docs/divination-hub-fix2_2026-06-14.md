# 排盘页面修复续 (2026-06-14)

## 本次修复

### PWA 安装提示条 - 已移除
用户反馈通过快捷方式打开时提示条仍然出现且不可用，按要求完全移除（HTML + CSS + JS）。

### 改名建议"分析姓名"功能 - 已修复
- **根因**：`KANGXI_STROKES` 康熙笔画字典仅 660 个字符，缺少大量常用字
- **影响**：`calculateWuge()` 对缺失字返回笔画=0，`analyzeName()` 判定为"生僻字"无法分析
- **修复**：扩充字典至 ~1280 个字符，覆盖数字、方位、吉祥字、常见人名字等
- **验证**：`computeRename()` 和 `analyzeName()` 在 Node.js 测试中均通过