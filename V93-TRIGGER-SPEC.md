# v9.3 训练触发规范（探针先行 · 阈值触发）

> 制定：2026-08-26 主会话 · 状态：生效中
> 取代事项：「v9.3 待用户触发」（旧 backlog 描述已废弃）
> 历史教训：v9.1 shuffle 破坏关联（45.1%）；v9.2 fused-as-base 续训致输出全空（3/488=0.6%），已归档废弃

## 一、生产基线

- 生产模型：**mingli-sft-v9.0-7b @ :8960**，BaziQA-488 = **263/488 = 53.9%**
- v9.2 已归档：`/Volumes/data1/ml-training/archive/mingli-sft-v9.2-7b-archived-fusedbase-broken`（不做任何重评——483/488 输出为空是确定性损坏，重评无信息增量）

## 二、触发阈值（两个条件同时满足才启动）

| 条件 | 阈值 | 检查方式 |
|---|---|---|
| 新 SFT 行为样本积累 | **≥ 500 条**（r754 复核病历→SFT 闭环产出，2026-08-26 基线为 0） | 每周检查 `case_review_logs` 转化量与 `training-data/` 新增 jsonl |
| 探针验证通过 | 见第三节，三项全过 | 触发时执行 |

未达到阈值前，任何「先训一版看看」的提议都应拒绝——v9.2 的 47 分钟训练 + 评估算力就是这么浪费的。

## 三、探针先行（触发时强制流程，约 15 分钟）

路径：**v8.7 base 直训**（严禁以任何 fused 模型为 base 做 LoRA 续训）

1. **低成本探针训练**：`training/mlx-models/mingli-sft-v8.7-7b` 为 base，iter ≤ 50，lr ≤ 2e-6，数据 = 新 SFT 样本
2. **local-q10 探针**：post-fuse 必跑，10 题中至少 8 题输出有效答案字母（v9.2 在此处 0/10 全空，可最早暴露空壳模型）
3. **抽样快评**：BaziQA-488 中固定抽样 50 题，正确率 ≥ v9.0 同抽样基线 − 3pp

三项全过 → 进全量训练（iter < 100，lr ≤ 2e-6，数据 = v9.0 原数据原样 + 新样本，**不 shuffle**）。
任一不过 → 中止，保留探针日志，回查数据/路径，不得强行全量。

## 四、全量训练后验收

1. BaziQA-488 全量评估，**> 53.9% 才考虑切换生产**
2. 切生产前跑完整 DISCLAIMER 链路检查（医疗/命理双边界）
3. 旧模型按归档约定移入 `/Volumes/data1/ml-training/archive/`，KANBAN 留痕

## 五、当前进度快照（2026-08-26）

- 新 SFT 样本积累：**0 / 500**（r754 闭环 08-26 上线，`case_review_logs` 仅 18 条待转化）
- 下次检查：随每周 KANBAN 日结更新本节数字
